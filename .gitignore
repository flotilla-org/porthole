/target
apps/macos/PortholeHelper/.build/
**/*.rs.bk
Cargo.lock.bak
.DS_Store
.idea/
.claude/
__pycache__/
# Test artefacts dropped at repo root by `porthole screenshot --out`,
# `porthole launch --json | jq` etc. during ad-hoc dogfooding.
/*.png
/*.json

# Private dependency checkout used by CI
/.deps/
