You are the coding agent in a real macOS desktop workflow verification. Work only in the current test evidence directory. Do not read identity.json, dump environment variables, or print PORTHOLE_AGENT_TOKEN. Your token is already inherited. Do not change code repositories, publish anything, or launch other agents.

Use the installed ~/.local/bin/porthole CLI to launch the real WorkflowEditor.app in this directory with --require-fresh-surface --json, Do not pass --cwd for this bundle launch. Pass the absolute path to editor-result.json as --arg. Save the returned JSON to editor-launch.json. Then use the returned surface_id with porthole text to type exactly: Porthole on Comte: input from the cleat agent. Save a screenshot using porthole screenshot SURFACE_ID --out editor.png. Read editor-result.json and confirm its text field matches the sentence, and record the PID and screenshot path in result.md. Keep the app open for visual verification and supervisor cleanup. Record the surface_id in state.json's surfaces array without removing other IDs. Do not edit identity.json.

If a command returns agent_permission_needed, report that and wait: the operator will approve via the existing CLI, after which retry the same command. Do not mint or approve your own grants. If system_permission_needed occurs, stop BLOCKED for the user. Ask for sandbox escalation for the installed CLI if necessary; do not disable the sandbox or work around daemon socket access. When done stay alive in the TUI so the supervisor can test detach/reattach to this same agent.
